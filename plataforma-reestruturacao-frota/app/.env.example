# Copie para .env.local e preencha.
# A service role key NUNCA deve ir para o front-end nem ser commitada —
# usar apenas em rotinas server-side específicas, e pedir ao usuário antes de usar.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
